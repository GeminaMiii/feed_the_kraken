# PROJECT_MAP — Feed the Kraken Online

خريطة المشروع الحالية (State Sync). تُحدَّث عند كل تغيير معماري.

## البنية

```
PRODUCT.md              ★ الهوية الاستراتيجية (المستخدمون، الشخصية، مبادئ التصميم)
DESIGN.md               ★ الهوية البصرية (الرموز اللونية، الخطوط، الحركة، بوابات الأداء)
server/
  index.js              السيرفر - Express + Socket.io (يخدم public/ ساكنة على المنفذ 3000)
                         + GET /api/stats — عدد الغرف/اللاعبين للصفحة الرئيسية
  game/
    GameRoom.js         محرك اللعبة - State Machine كامل
    constants.js        الجداول: تركيبة الفرق، عتبات المطيانة، مجموعات الأوراق
    mapLong.js          خريطة Feed the Kraken (31 خلية + 7 وجهات نصر)
                         مصدر الحقيقة: map_f1.html (topology + navigation + actions + victory)
                         نظام الإحداثيات: h{row}_{x} (row = floor(y)+1، 1..7 جنوب→شمال، x إزاحة أفقية)
    characters.js       بطاقات الشخصيات الـ 22
public/
  site.css              ★ نظام التصميم المشترك (الرموز، الأزرار، الرقاقات) — مرجعه DESIGN.md
  sea-stage.css         ★ عالم البحر المشترك (CSS فقط) — تستهلكه index/narrator/player
  icons.js              ★ الأيقونات المشتركة (sprite بخط 1.5) — لا إيموجي في أي واجهة
  index.html            ★ الصفحة الرئيسية الجديدة: هيرو عاصفة + قصة تمرير + معرض شخصيات + دخول
  storm.js              ★ مشهد العاصفة WebGL (وحدة ESM) — سطح المكتب فقط
  vendor/               ★ three 0.185.1 + gsap 3.15.0 محليًا (لا CDN — جلسات على شبكة محلية)
  player.html           ★ واجهة اللاعب (موبايل) — مُهاجَرة إلى site.css (v0.13)
  player.css            ★ طبقة اللاعب فوق site.css — لا رموز لونية جديدة
  display.html          شاشة العرض المشتركة (تلفزيون)
  narrator.html         لوحة الراوي
  ftk-map.js            ★ راسم الخريطة المشترك (Canvas) — الطبقة المشتركة/Core
test/
  sim.js                اختبار محاكاة شامل (210 لعبة) - npm test
  e2e.js                اختبار E2E شبكي (يحتاج سيرفر)
  socket-3p-test.js     اختبار سيناريو 3 لاعبين
  ftk-map-test.js       ★ اختبار راسم الخريطة (TDD، vm + Canvas وهمي)
```

## طبقة الواجهة الجديدة (v0.11 → v0.13)

`site.css` هو المصدر الوحيد للرموز البصرية. المُهاجَرون: `index.html` (v0.11) ثم
`narrator.html` ثم `player.html` (v0.13). **الباقي: `display.html` وحدها.**

**قاعدة اللون الحاكمة:** الألوان المشبعة الوحيدة في المنتج هي الفصائل الثلاثة
(`--sailor` أزرق شرق · `--pirate` أحمر غرب · `--cult` أصفر شمال · `--cultist` أخضر).
كل ما عداها حبر (`--ink-*`) وعظمي (`--bone*`). أُلغي النحاسي/الذهبي القديم لأنه
كان يتصادم مع أصفر الطائفة ويُفسد الإشارة.

**`storm.js`** يُحمَّل فقط إن اجتاز الجهازُ البوابات (مؤشر دقيق، عرض ≥ 720،
ذاكرة ≥ 4غ، أنوية ≥ 4). خلاف ذلك يبقى بديلُ CSS المبنيُّ أولًا في `index.html`
— وهو مشهد كامل (سفينة SVG + أمواج + مطر + برق) لا صفحةٌ فارغة.
`?nogl=1` يفرض البديل لفحصه على سطح المكتب.

**اتجاهات الخريطة:** الشرق (بحّارة) يمينَ الشاشة والغرب (قراصنة) يسارَها —
في RTL يعني ذلك أن عنصر الشرق يأتي **أولًا** في HTML. يطابق ذلك `ftk-map.js`
(x موجب = شرق) والمشهد ثلاثي الأبعاد (بحّار عند X+).

## الطبقة المشتركة/Core: `public/ftk-map.js`

وحدة `window.FTKMap` تكشف عقدة ثابتة:
- `FTKMap.build(el, opts?)` — `el` هو `<svg id="mapSvg">`؛ تُستبدل بـ `<canvas>`
  في مكانها (نفس المعرف) فتبقى الصفحات تعمل دون تعديل. `opts.onHexClick` اختياري.
- `FTKMap.update(el, state)` — `state` من `publicState()`: `{shipHex, usedMapActions, mode, ...}`.

المستهلكون (نفس الاستدعاء حرفياً):
- `display.html:137,164`  — `FTKMap.build($('mapSvg'))` / `FTKMap.update($('mapSvg'),S)`
- `player.html:203,288`   — نفسه
- `narrator.html:249,345` — نفسه (مراقب فقط)

منطق الرسم (المسدسات + الأسهم المزدوجة + السفينة ⛵) مأخوذ من ملف HTML المرفق
(Feed the Kraken hexagonal map) ويعمل على Canvas 800×650 بـ 31 عقدة بنظام `{col,y}`.

## طبقة الترجمة: `num` ↔ `h{row}_{x}`

`map_f1.html` يرقّم الخلايا بـ `num` (1..31) ويحدد موقعها بـ `{col, y}` (y ∈ {0.0, 0.5, …, 6.0}).
السيرفر يستخدم `h{row}_{x}` حيث:
- `row = Math.floor(y) + 1` (صفوف 1..7؛ يبقي `SUPPLY_LINE_AFTER_ROW = 4` وكل فحوص `row > 4`
  في `GameRoom.js` تعمل دون تعديل — الانتقال y ≤ 3.5 → y ≥ 4.0 يعادل row 4 → row 5).
- `x = col` (سالب = غرب/قراصنة، موجب = شرق/بحّارة).

`mapLong.js → NODES` يعرّف العلاقة 1:1 (`num ↔ {col, y} ↔ h{row}_{x}`)، و`EXIT_TABLE[num]`
يحوي الأهداف بأرقام `num` أو ثوابت `VICTORY.*`. `computeExits(num)` يحوّلها إلى `h{row}_{x}`
عند بناء الخريطة. `serializeMap(map)` يُرسل `y` و`num` للعميل كي يرسم الموضع والرموز.

## مناطق النصر (7 خلايا طرفية — كل exits → VICTORY ثابت)

| num | hexId   | الفصيل | اللون |
|---|---|---|---|
| 31 | h7_0    | الكراكن 🦑   | `CULT_VICTORY`   |
| 22 | h5_-3   | القراصنة 🏴‍☠️ | `PIRATE_VICTORY` |
| 26 | h6_-2   | القراصنة 🏴‍☠️ | `PIRATE_VICTORY` |
| 29 | h6_-1   | القراصنة 🏴‍☠️ | `PIRATE_VICTORY` |
| 25 | h5_3    | البحّارة ⚓   | `SAILOR_VICTORY` |
| 28 | h6_2    | البحّارة ⚓   | `SAILOR_VICTORY` |
| 30 | h6_1    | البحّارة ⚓   | `SAILOR_VICTORY` |

## التغيير الأخير: إعادة كتابة كاملة للخريطة — map_f1.html كمصدر وحيد للحقيقة

**السياق:** محاولة سابقة لتعديل جراحي على `ftk-map.js` فشلت لافتراضات خاطئة عن الطوبولوجيا.
الحل الصحيح: `map_f1.html` هو المصدر الكنسي للطوبولوجيا، الملاحة، توزيع الأكشنز، وشروط النصر.

**التغييرات:**
- `server/game/mapLong.js`:
  - `ROW_X` جديد بأحجام [3, 3, 5, 7, 7, 5, 1] = 31 خلية (مطابق لـ map_f1).
  - `NODES` (31 إدخال `{num, col, y}`) + `EXIT_TABLE[num]` محسوب مسبقاً من `getNextNode` في map_f1.
  - `computeExits(num)` يحوّل أرقام num إلى `h{row}_{x}` (أو يمرّر `VICTORY.*`).
  - `ACTION_PLACEMENT` جديد (10 أكشنز: cabin_search 4x، off_with_tongue 1x، flogging 2x، feed_the_kraken 3x).
  - `isValidExit(fromHex, dest, map)` جديد: يتحقق أن `dest` داخل `fromHex.exits` (أو victory) — لا قيد `row+1` صارم.
  - `serializeMap(map)` يُضيف `y` و`num` للعميل.
  - **توحيد الخرائط:** `buildQuickJourneyMap()` ترجع نفس 31 خلية كـ `buildLongJourneyMap()`.
    أُلغي `ROW_X_QUICK` و`ACTION_PLACEMENT_QUICK` (Quick = Long طوبولوجياً).
- `public/ftk-map.js`:
  - `NODES` (31 إدخال) + `EXIT_TABLE` مطابقة للسيرفر.
  - `pixelOf(hex)`: `cy = (height - 80) - hex.y * hexHeight` — مباشرة من `y` (لا من `row`) لتفادي الانسطاح البصري.
  - رموز الأكشن (🔍🗡️📖🐙) من `hex.action`، مع تعتيم `globalAlpha = 0.35` عند `usedMapActions`.
  - مناطق النصر ملونة (🦑 حدّ أصفر / 🏴‍☠️ حدّ أحمر / ⚓ حدّ أزرق).
  - تخطيط واحد لكل الأوضاع (لا mode switching) بعد توحيد Quick = Long.
- `test/ftk-map-test.js`: تأكيدات جديدة على الطوبولوجيا، المخارج، الأكشنز، مناطق النصر،
  وتكامل GameRoom (92/92 GREEN).
- `test/sim.js`: لم يُمَسّ — 210/210 GREEN مع الخريطة الموحدة (Quick = Long).

## الدين التقني (Technical Debt)

| المعرف | الوصف | تأثير | التسجيل |
|---|---|---|---|
| ~~TD-1~~ | ~~`usedMapActions` غير مرسوم بصرياً~~ ✓ مُعالَج: رموز الأكشن (🔍🗡️📖🐙) مرسومة من `hex.action` مع تعتيم `globalAlpha=0.35` عند `usedMapActions`. | — | — |
| ~~TD-2~~ | ~~`mode` (quick/long) لا يغيّر التخطيط~~ ✓ مُعالَج: توحيد كامل — `buildQuickJourneyMap()` ترجع نفس 31 خلية كـ Long. Quick و Long طوبولوجياً متطابقتان. | — | — |

## فجوات القواعد (Rule Gaps)

| المعرف | الوصف | الحالة |
|---|---|---|
| ~~R64~~ | ~~قلة اللاعبين (دون 3) لا تُملأ عشوائياً — `appointTeam()` تتجمد~~ ✓ مُعالَج: `appointTeam()` يكتشف `< 2` لاعب مؤهل ويستدعي `_fillPositionsRandomly()` لسحب ورقتين عشوائياً لكل مركز شاغر (Lieutenant/Navigator) وتنفيذ ورقة الملاحة مباشرةً. تمت إضافة `_drawTwoAndPick()` وحماية بحث `ltCount` في `_startNavigation()` ضد `null`. | مُغلَق |

## سجل التغييرات (Changelog)

### v0.13 — إعادة تصميم واجهة اللاعب

- **الهجرة:** `player.html` تركت `:root` المحلّي (ألوان hex وخط Tajawal ونحاسٌ
  كلونِ واجهة — مخالفٌ صريح لـ`DESIGN.md §Color`) وصارت تبني فوق
  `site.css` + `sea-stage.css` + **`player.css` الجديد**. نفس البحر ونفس
  الزجاج ونفس عتاد النحاس بزاويتين المستعملة في لوحة الراوي.
- **محرّك تحديثٍ موضعيّ:** لا `innerHTML` لإعادة بناء أي جزءٍ حيّ. صفوف الطاقم
  تُسوَّى صفًّا صفًّا (`id → DOM`)، وأيقونات المسدسات تُضاف/تُزال فرديًّا،
  ولوحة الإجراء تُقارَن ببصمةٍ فلا تُعاد إلا عند تغيّرٍ حقيقي. التركيزُ
  وقيمُ المنسدلات تُحفظ وتُستعاد.
- **قسم المناصب** (قبطان · مساعد أول · ملّاح) كتلةٌ منفصلة فوق قائمة طاقمٍ
  عمودية دائمة الظهور — حُذف الشريط الأفقي `.crew{overflow-x:auto}` كليًّا.
  الاسم بلا رقم مقعد، والطلقاتُ أيقوناتٌ مكرّرة لا رقمٌ مجرّد.
- **شارات الانتماء** تُشتقّ من `privateState()` وحده (`knownPirates` ·
  `knownCultLeader` · `knownCultists`) وعند `game_over` — لا تسريبَ لما
  يُخفيه الخادم.
- **رصيفُ إجراءاتٍ سفليّ** لا يختفي أبدًا (يتحوّل إلى حالة انتظارٍ صريحة)،
  يحترم `env(safe-area-inset-bottom)`، وارتفاعُه محجوزٌ عبر `--dock-h`
  يقيسه `ResizeObserver`. على سطح المكتب يُحاذي عمودَ الإجراء بالحساب نفسه.
- **الحركات:** FLIP يعبر كتلتَي المناصب والطاقم (الترقيةُ صعودٌ حقيقي لا
  اختفاءٌ وظهور) · نقصانُ الطلقة يتلاشى قبل تحديث الرقم · انتقالُ المرحلة
  بطبقتين · كشفُ الهوية بتكبيرٍ ووهجٍ ثمّ استقرار · تمدّدُ الخريطة قبل
  ظهورها · قلبُ ورقة الملاحة ثلاثيَّ الأبعاد · إضاءةُ الرصيف **مرّةً واحدة**
  عند بدء الدور. كلُّها ≤600ms وتُلغى تحت `prefers-reduced-motion`.
- **نافذةٌ واحدة** `showModal()` حلّت محلّ خمس نوافذ متطابقة، وحلّت محلّ
  `confirm()` الأصلي مرّتين. حُذفت ثلاثة مؤقّتات ميتة لم تكن تُشغَّل أبدًا.
- **سجلّ الأسرار** صار خطًّا زمنيًّا مؤرَّخًا بالجولة.
- **صفر إيموجي** في الواجهة: ~٦٠ إيموجي استُبدلت بأيقونات `icons.js`
  (أُضيفت إليه ١٦ رمزًا جديدًا — إضافةٌ لا تعديل).
- **`GameRoom.js`:** أُضيف `this.round` (يزيد في `_finishRound()`) وحقلُ
  `round` في `publicState()` — حقلُ عرضٍ للقراءة فقط، لا يدخل في أي قاعدة.
- **الوصولية:** أُزيل `user-scalable=no` و`html{font-size:15px}`؛ أهدافُ اللمس
  كلُّها ≥44px؛ المنسدلات ≥16px خطًّا (تمنع تكبير iOS)؛ أسماءُ اللاعبين
  تُهرَّب في كل موضعٍ يدخل الترميز.
- **ما لم يُمَسّ:** كلُّ أحداث Socket وحمولاتها · منطقُ كل مرحلة · جداولُ
  الترجمة · حجابُ الهوية ومؤقّته · عتبةُ التمرّد · `FTKMap.build/update` ·
  **وكلُّ قوائم `<select>` ومعرّفاتها** (`ltSel` · `navSel` · `chT` · `chT2` ·
  `chR` · `emNav` · `maT` · `mmT` · `tsT` · `cvT` · `gs1-3`).
- **التحقّق:** `npm test` 210/210 · `ftk-map-test` 84/84 · لعبةٌ كاملة بـ7
  لاعبين عبر كل المراحل حتى `game_over` · تباين ٩ ألوان × ٣ أسطح كلُّها
  ≥4.5:1 (صفر إخفاقات) · لا تجاوز أفقي على 375/768/1280 · صفوف الطاقم
  لا يُعاد إنشاؤها عبر رسماتٍ متتالية · التركيز وقيمُ المنسدلات تنجو من
  إعادة البناء.

### v0.12 — لوحة كحلية بحرية + رفع إضاءة المشهد + نصّ عربي جديد
- **اللون:** استُبدلت القاعدةُ الرماديةُ الباردة (hue 205، chroma 0.018) بكحليٍّ
  بحريٍّ حقيقي (hue 230–235، chroma 0.045–0.052). `--bone*` الدافئة صارت
  `--mist*` باردةً مائلةً للأزرق كي تنسجم مع القاعدة الزرقاء. أُضيف `--cyan`
  (hue 205) للمسات التفاعلية — وهيؤه يبعده عن أزرق البحّارة (242) فلا يُقرأ
  إشارةَ فصيل.
- **إضاءة المشهد:** رُفعت بوضوح مع بقاء التباين السينمائي — ضوء محيط
  0x35505e/1.4 → 0x51789b/2.4، قمر 0.8 → 1.9، وضوء حافة جديد يفصل السفينة
  عن الأفق. ألوان الماء والسماء وخامات السفينة رُفعت كلُّها. الكراكن صار
  0x08131f بدل الأسود الخالص (الأسودُ التامّ صار ينشزُ كثقبٍ مقصوص بعد رفع
  إضاءة السماء).
- **تأطير السفينة:** نُقلت إلى (−9, 0, −26) بتحجيم 1.7 — كانت مقصوصةً عند
  حافة الإطار.
- **الخطوط:** Reem Kufi → **Alexandria** للعناوين (حروفٌ مفتوحة أوضح في
  العناوين الطويلة والنصّ المشكول). النص بقي IBM Plex Sans Arabic لكن
  رُفع إلى 17px، وارتفاع السطر إلى 1.95، وعرض السطر ضُيِّق إلى 48–54ch.
- **النصّ العربي:** استُبدل كاملًا بنصٍّ سرديٍّ أطول بإيقاعٍ شعريّ (أسطر
  `.beats`)، وأسماءُ الفصائل مُلوَّنةٌ داخل الجُمل نفسها.
- **إصلاحات كشفها النصّ الجديد:**
  - الهيرو كان يفتقر لحشوةٍ علوية فتسلّق العنوانُ الطويل فوق الشريط الثابت.
  - `--fs-display` حدُّه الأدنى 2.6rem كان يكسر أطولَ سطرٍ سطرَين على 375px.
  - البديل على الجوال: خطُّ الماء نزل إلى 20% (كان 46%) — النصُّ الطويل صار
    يمرّ فوق البحر فتسوء قراءتُه.
  - `--pirate` رُفع إلى L=0.68 (كان 4.42:1 على ink-700).
  - أُزيل `THREE.Clock` المهجور (كان يغرق الطرفية بالتحذيرات).
- **التحقق:** `npm test` 210/210 GREEN. تباين: ٨ ألوان × ٣ أسطح كلها ≥ 4.5:1.
  المشهد ~60 إطار/ثانية. لا تجاوز أفقي، كل أهداف اللمس ≥ 44px.

### v0.11 — إعادة بناء الصفحة الرئيسية بالكامل
- **الملفات الجديدة:** `PRODUCT.md`، `DESIGN.md`، `public/site.css`، `public/storm.js`، `public/vendor/`.
- **`public/index.html`:** أُعيد بناؤها من الصفر (كانت 26 سطرًا من روابط). الآن:
  هيرو عاصفة ثلاثي الأبعاد، قصة تمرير بخمس خطوات مربوطة بأطوار اللعبة الحقيقية،
  معرض بطاقات الشخصيات الـ21 (كانت أصولًا مهدورة)، ودخول مدمج للأدوار الثلاثة.
- **`server/index.js`:** أُضيفت `GET /api/stats` (عدد الغرف واللاعبين). لا تكشف
  أي معلومة عن هوية لاعب أو دوره.
- **`public/player.html`:** أُضيف قراءة `?code=&name=` من نموذج الصفحة الرئيسية
  والانضمام التلقائي. لم يُمَسّ أي منطق لعب أو حدث Socket.
- **التحقق:** `npm test` 210/210 GREEN. `node test/ftk-map-test.js` 82/82 GREEN.
  تباين اللون: ٧ ألوان × ٣ أسطح كلها ≥ 4.5:1 (رُفع `--bone-mut` من L=0.58 إلى 0.64،
  و`--pirate` من L=0.63 إلى 0.66 بعد قياس فعلي في المتصفح).
  المشهد ثلاثي الأبعاد: ~60 إطار/ثانية.
- **مشكلة سابقة (غير متعلقة):** `test/ftk-map-test.js` ينجح 82/82 لكنه لا يخرج
  من العملية (مؤقّت متبقٍّ في `GameRoom`) — يحتاج `--forceExit` أو تنظيف المؤقّتات.

### v0.10 — R64: ملء المراكز الشاغرة عشوائياً عند قلة اللاعبين
- **المشكلة:** عند انخفاض عدد اللاعبين على السفينة دون 3، كان `appointTeam()` في `server/game/GameRoom.js` يطرح خطأً (أو يتجمد) لعدم وجود لاعبَين مؤهلَين لـ Lieutenant و Navigator، دون مسار تعافٍ.
- **الإصلاح (قواعد R64):** إن قلّ عدد اللاعبين الأحياء غير القبطان وغير Off-duty عن اثنين، يتحول المحرك إلى ملء عشوائي:
  - `appointTeam()` يكتشر الحالة مبكراً ويستدعي `_startNavigation()` ثم `_fillPositionsRandomly()`.
  - `_fillPositionsRandomly()`: يُبقي ورقة القبطان عشوائياً، ثم يسحب ورقتين لكل مركز شاغر (عبر `_drawTwoAndPick()`) ويُبقي واحدة في الـ Logbook ويرمي الأخرى، ثم يخلط الـ Logbook وينفّذ ورقة عشوائية.
  - `_startNavigation()`: حماية `ltCount` ضد `null` (`this.lieutenantId ? … : 0`) وإضافة علمَي `ltFilledRandomly`/`navFilledRandomly` لحالة `this.nav`.
- **التحقق:** سيناريو 7 لاعبين → إقصاء 5 → قبطان + لاعب واحد → `appointTeam()` يملأ عشوائياً ويُكمل دون تجميد. `npm test` (210/210) و `node test/ftk-map-test.js` (90/90) GREEN بلا تغيير على المسار الطبيعي.

## التحقق (Verification)

- `node test/ftk-map-test.js` — 92/92 (طوبولوجيا map_f1 + المخارج + الأكشنز + مناطق النصر + تكامل GameRoom + أداة الراوي move_ship).
- `npm test` (sim.js) — 210/210 بلا تغيير (الراسم غير متورط في المحاكاة؛ الخريطة الموحدة Quick=Long تعمل لكل عدد لاعبين 5..11).
- استدعاءات الصفحات الثلاث (`FTKMap.build($('mapSvg'))` / `update`) لم تُمَسّ.

## التشغيل

```bash
npm install
npm start        # السيرفر على المنفذ 3000
npm test         # sim.js (210 لعبة)
node test/ftk-map-test.js   # اختبار الراسم
```
